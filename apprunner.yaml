version: 1.0
runtime: nodejs18
build:
  commands:
    pre-build:
      - cd backend
    build:
      - npm install
run:
  command: node backend/index.js
  network:
    port: 3001
  variables:
    NODE_ENV: production
